# Application Configuration
APP_ENV=development
APP_PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=yourapp
DB_SSLMODE=disable

# AWS Cognito Configuration (for RBAC authentication)
COGNITO_REGION=us-east-1
COGNITO_USER_POOL_ID=

# JWT Configuration (standard variable names for JWT validation)
JWT_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX
JWT_AUDIENCE=
JWKS_URI=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_XXXXXXXXX/.well-known/jwks.json

# For local testing with cognito-local, leave COGNITO_USER_POOL_ID empty
# The mock middleware will be used for development
# For production, get the User Pool ID from AWS Console or Terraform output
# JWT_ISSUER should match: https://cognito-idp.{COGNITO_REGION}.amazonaws.com/{COGNITO_USER_POOL_ID}
# JWKS_URI should match: https://cognito-idp.{COGNITO_REGION}.amazonaws.com/{COGNITO_USER_POOL_ID}/.well-known/jwks.json
